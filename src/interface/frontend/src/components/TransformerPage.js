import React, { Component } from 'react';

export default class TransformerPage extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <p>This is the TransformerPage</p>;
    }
}