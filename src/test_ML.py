# %%
from imports.HI_calculation.algorithms.method_5 import training, inference, plot_inference

#perf=training(debug=True)
#print(perf)
#pred=inference(debug=True)
#print(pred)
# %%
#plot_inference(chosen_trs='SE4')

# Show tensorboard
# tensorboard --logdir logs/fit

